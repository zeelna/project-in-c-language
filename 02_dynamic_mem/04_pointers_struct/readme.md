// Step #1: Compile code into binary
gcc -prog -fsanitise=address 04_pointers_struct.c

// Step #2: Run the binary to read both files and output tax percentages. 
Usage: ./prog person.txt vehicles.txt


